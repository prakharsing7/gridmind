"""
Future feature roadmap for gridmind v2.0+.

v2.0 planned features:
- V2G support (bidirectional ChargingProfile with negative power periods)
- Rolling horizon optimisation (re-optimise every 15 minutes with updated SoC)
- Stochastic optimisation (uncertain departure times, price uncertainty)
- Grid frequency signal integration (OCPP 2.0.1 frequency service)
- Integration with PyPSA for distribution network power flow validation
"""

# This module is intentionally empty — it documents planned features only.
